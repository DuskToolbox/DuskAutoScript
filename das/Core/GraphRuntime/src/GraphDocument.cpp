#include <das/Core/GraphRuntime/GraphDocument.h>

// yyjson caster specializations and field_name_rule registrations are
// defined inline in GraphDocument.h following the project's DTO pattern
// (see TaskRepositoryDtos.h).  This translation unit exists so that
// das_add_core_component(GraphRuntime) picks up a source file.
