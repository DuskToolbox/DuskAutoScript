#include <das/Core/GraphRuntime/CompiledArtifact.h>

// yyjson caster specializations and field_name_rule registrations are
// defined inline in CompiledArtifact.h following the project's DTO pattern
// (see GraphDocument.h).  This translation unit exists so that
// das_add_core_component(GraphRuntime) picks up a source file.
